# random-draggable-boxes

Source: Google Drive spreadsheet `feature requests.xlsx`.

Request: "random draggable boxes into the centre" for Exquisite Stories.

Flesh out the intended behavior before implementation:
- where random boxes originate from;
- how many boxes should be generated;
- whether this applies to new sessions, a button/action, or the existing drag flow;
- how this should behave on touch devices and small screens.
