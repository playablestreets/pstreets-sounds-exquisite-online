# readme-empty

**cleanup** — README is essentially empty.

`README.md` currently:

```
# pstreets-sounds-exquisite-online
🙉
pip was here
```

For a project that's deployed and links out to `playableweb.com` and a JotForm DIY page, a real README would help future maintainers and contributors. Suggested contents:

- One-paragraph "what this is" (Three.js exquisite-corpse cube app pulling stories from Prismic).
- How to run locally (it's static — `python -m http.server` from `public/`?).
- Where the content comes from (Prismic repo `playable-web`, type `sounds_exquisite`).
- Link to the DIY contribution form.
- Deployment notes if any.

The page title says `Exquisite Stories v2.7` — the README could match the user-facing name too.
